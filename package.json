{
  "name": "x-dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    
    "dev": "browser-sync start --server --files index.css",
    "share": "browser-sync start --proxy \"index.html\" --files index.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^3.0.2"
  },
  "devDependencies": {
    "webpack-cli": "^4.10.0"
  }
}
